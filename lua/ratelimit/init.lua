return {
  debounce_leading = require("ratelimit.debounce").leading,
  debounce_trailing = require("ratelimit.debounce").trailing,
  throttle_leading = require("ratelimit.throttle").leading,
  throttle_trailing = require("ratelimit.throttle").trailing,
}
